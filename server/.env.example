# Server Environment Variables

# ============================================================
# DATABASE CONFIGURATION
# ============================================================

# OPTION 1: SQLite (Recommended for local development)
# Simple file-based database, no setup required
DATABASE_URL="file:./dev.db"

# OPTION 2: PostgreSQL (Required for Render deployment)
# Use this for production or if you want to match Render environment locally
# DATABASE_URL="postgresql://user:password@localhost:5432/funnyfriends?schema=public"

# ============================================================
# SECURITY CONFIGURATION
# ============================================================

# JWT Secret - Generate a strong secret for production!
# You can generate one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# ============================================================
# DEVELOPMENT CONFIGURATION
# ============================================================

# Client URL (for CORS in development)
CLIENT_URL="http://localhost:5173"

# Server Port
PORT=3000

# Node Environment
development

# ============================================================
# RENDER DEPLOYMENT NOTES:
# ============================================================
# 1. Use PostgreSQL (see POSTGRESQL_SETUP.md)
# 2. DATABASE_URL will be auto-set if using Blueprint
# 3. JWT_SECRET should be generated (Render can do this)
# 4. NODE_ENV should be set to "production"
# ============================================================
